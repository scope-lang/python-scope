var sqr=(a,i)->{
  if(i<1){
    return 1;
  }
  var est=sqr(a,i-1);
  est=a/est+est;
  return est/2;
};
var ans=sqr(5,100)/2 + (1/2);
console.log(ans);
console.log(0.5);
console.log(Math.pow(2,0.5));
console.log(Math.E);
console.log(Math.abs(-1));
console.log(Math.acos(Math.pow(0.5,0.5))*4);
